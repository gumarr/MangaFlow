<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="MangaFlow.App.Views.SelectionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MangaFlow.App.Views"
    mc:Ignorable="d"
    Title="Select Screen Region">

    <views:CursorGrid x:Name="RootGrid"
          PointerPressed="OnPointerPressed"
          PointerMoved="OnPointerMoved"
          PointerReleased="OnPointerReleased"
          KeyDown="OnKeyDown">
        
        <Grid>
            <Image x:Name="BackgroundScreenImage" Stretch="Fill" />
            <Grid Background="#40000000" />
        </Grid>

        <Canvas x:Name="SelectionCanvas" Background="Transparent">
            <Rectangle x:Name="SelectionRect"
                       Stroke="#FFFF0000"
                       StrokeThickness="2"
                       StrokeDashArray="4 2"
                       Fill="#20FF0000"
                       Visibility="Collapsed" />
        </Canvas>
    </views:CursorGrid>
</Window>
