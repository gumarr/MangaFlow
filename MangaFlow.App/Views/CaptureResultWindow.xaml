<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="MangaFlow.App.Views.CaptureResultWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MangaFlow.App.ViewModels"
    mc:Ignorable="d"
    Title="Screen Capture Result">

    <Grid Padding="24" RowDefinitions="Auto, *, Auto" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!-- Title and Metadata -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <TextBlock Text="Captured Region" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold" />
            <Grid ColumnDefinitions="Auto, *">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE740;" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        <TextBlock Text="Dimensions:" FontWeight="SemiBold" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        <TextBlock Text="{x:Bind ViewModel.Dimensions, Mode=OneWay}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE81C;" FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        <TextBlock Text="Captured At:" FontWeight="SemiBold" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        <TextBlock Text="{x:Bind ViewModel.Timestamp, Mode=OneWay}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </StackPanel>

        <!-- Image Preview Frame -->
        <Border Grid.Row="1" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image Source="{x:Bind ViewModel.CapturedImage, Mode=OneWay}" 
                       Stretch="None" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" />
            </ScrollViewer>
        </Border>

        <!-- Footer Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Close" Click="OnCloseClick" Width="100" Style="{ThemeResource AccentButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
